// lib
import { FC } from "react";

// components
import { Authorization } from "./components/auth/Authorization.component";

const App: FC = () => {
  return (
    <div className="app">
      <Authorization />
    </div>
  );
};

export default App;
