import { Request, Response } from "express";

export class AdminController {
    public async adminAuth (req: Request, res: Response) {
        try {

        } catch (e) {
            console.log(e);
        }
    }
}